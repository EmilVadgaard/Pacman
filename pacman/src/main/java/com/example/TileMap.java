package com.example;

public class TileMap {
    
    public void tileMap(Grid grid){
        for (int y = 0; y < grid.getMap().length; y++){
            for (int x = 0; x < grid.getMap()[0].length; x++){

            }
        }
    }

    //public void
}
